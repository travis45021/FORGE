name: FORGE integrity

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.14"]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install FORGE and validation tools
        run: python -m pip install --upgrade pip && python -m pip install -e ".[validation]"
      - name: Run FAS and repository integrity tests
        run: python -m unittest discover -s tests/fas -p "test_*.py"
      - name: Compile Python sources
        run: python -m compileall -q src tests
      - name: Verify dependency health
        run: python -m pip check
      - name: Build wheel
        run: python -m pip wheel . --no-deps --no-build-isolation --wheel-dir dist
